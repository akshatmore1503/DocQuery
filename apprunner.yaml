# apprunner.yaml
version: 1.0
runtime: python3.11 # or python3.10, etc.
build:
  commands:
    build:
      - pip install -r requirements.txt
run:
  command: streamlit run app/app.py --server.port 8080 --server.headless true
  network:
    port: 8080